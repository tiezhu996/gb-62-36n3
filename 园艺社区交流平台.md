## 项目主要需求

### 项目概述

搭建一个园艺爱好者社区交流平台，用户可以分享自己的花园日记、种植经验、花艺作品，与其他爱好者互动交流，形成活跃的园艺社交生态。

### 功能需求

1. **花园日记发布**：用户可创建图文日记记录植物生长过程，支持多图上传、标签分类（播种、发芽、开花、收获等），日记按时间线展示。
2. **话题广场**：按热门话题（阳台花园、庭院设计、室内绿植、水培、堆肥等）聚合内容，用户可参与话题讨论和发布话题帖子。
3. **花友圈动态**：类似朋友圈的动态流，用户可发布简短的种植动态（图文），关注的花友动态实时出现在信息流中，支持点赞、评论、转发。
4. **用户关注与粉丝体系**：支持关注/取消关注其他用户，查看关注列表和粉丝列表，个人主页展示该用户的花园日记和动态。
5. **积分与等级系统**：用户通过发帖、评论、签到获得积分，积分对应不同等级（种子、幼苗、花朵、参天大树），等级展示在用户头像旁。
6. **活动与挑战**：平台定期发起种植挑战活动（如"30天多肉养成记"），用户可报名参与并提交成果照片，活动结束后评选优秀作品。
7. **私信系统**：用户之间可发送私信进行一对一交流，支持文字和图片消息，有新消息未读提醒。
8. **内容举报与管理**：用户可举报不良内容，管理员可审核举报并对违规内容进行删除、警告等处理。

### 技术要求

- 前端：Next.js 14 + TypeScript + Tailwind CSS，构建工具 Next.js 内置
- 后端：Node.js + Express + TypeScript，ORM 使用 Prisma
- 数据库：MongoDB（文档存储，适合动态和日记等内容）
- 缓存：Redis（用于会话、积分计算、消息队列）
- 实时通信：WebSocket（用于私信和通知推送）
- 认证：JWT + OAuth2（支持微信登录）
- 图片存储：MinIO 对象存储

## 代码存放规则

这是前后端分离项目。

前端代码放在 `frontend/`：

页面，组件，路由,状态管理,API 请求,类型定义,样式和资源等放在对应的子文件夹

后端代码放在 `backend/`：

数据库脚本放在 `database/`。

## 部署要求

项目必须支持 Docker Compose 一键部署。

需要提供以下文件：

- 根目录 `docker-compose.yml`：编排前端、后端、数据库及所有依赖服务
- `frontend/Dockerfile`：前端项目构建与运行镜像（多阶段构建，先构建再用 Nginx 托管）
- `backend/Dockerfile`：后端项目构建与运行镜像
- `frontend/nginx.conf`：Nginx 配置，处理前端路由和 API 反向代理
- `.env.example`：所有环境变量的示例文件，附注释说明

docker-compose.yml 要求：

- 所有服务使用命名容器，便于管理
- 数据库数据使用命名卷持久化
- 前端通过 Nginx 反向代理后端 API
- 服务间通过 Docker 内部网络通信
- 数据库服务配置 healthcheck，后端服务依赖数据库健康检查通过后再启动
- 端口映射（本项目分配的端口）：
  - 前端：8103:80
  - 后端：3103:3000
  - MongoDB：2903:27017
  - Redis：6503:6379
- 支持通过 `docker compose up -d` 一条命令启动全部服务

## README 写法

根目录必须有 `README.md`，用中文说明项目。

README 包含：

- 项目名称和一句话介绍
- 项目主要功能
- 快速启动方式（Docker Compose 一键部署命令）
- 本地开发方式（前端启动命令、后端启动命令）
- 访问地址
- 技术栈
- 项目目录结构
- 环境变量说明
- Docker 部署说明（端口映射、数据卷、常见问题）
- License

README 要求：

- Docker 部署命令放在最前面，作为首选启动方式
- 本地开发方式作为备选
- 命令可直接复制运行
- 目录结构用代码块展示
- 技术栈用表格展示
- 不要保留脚手架默认 README 内容

